package com.hospital.hms.mpiintegration.entity;

/**
 * Alias Type enumeration for patient name variations
 * 
 * @author HMS Enterprise Team
 * @version 1.0.0
 */
public enum AliasType {
    MAIDEN_NAME,
    NICKNAME,
    PREVIOUS_NAME,
    PREFERRED_NAME,
    LEGAL_NAME,
    ALIAS,
    STAGE_NAME,
    PROFESSIONAL_NAME,
    RELIGIOUS_NAME,
    OTHER
}
