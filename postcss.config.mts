/**\n * PostCSS Configuration - TypeScript Module Edition\n * Hospital Management System\n * \n * Modern PostCSS configuration with TypeScript support for healthcare\n * application styling, optimized for Tailwind CSS and autoprefixing.\n * \n * Features:\n * - Tailwind CSS integration\n * - Autoprefixer for cross-browser compatibility\n * - TypeScript type definitions\n * - Enterprise CSS optimization\n * - Healthcare-specific styling support\n * \n * @version 2.0.0\n * @author HMS Development Team\n * @compliance Enterprise CSS Standards, Healthcare UI Guidelines\n */\n\nimport type { Config } from 'postcss-load-config';\nimport autoprefixer from 'autoprefixer';\nimport tailwindcss from 'tailwindcss';\n\n/**\n * PostCSS configuration for healthcare application styling\n */\nconst config: Config = {\n  plugins: \n    // Tailwind CSS for utility-first styling\n    tailwindcss: {},\n    \n    // Autoprefixer for cross-browser compatibility\n    autoprefixer: {\n      // Target browsers for healthcare applications\n      overrideBrowserslist: [\n        '> 1%',\n        'last 2 versions',\n        'Firefox ESR',\n        'not dead',\n        'not IE 11',\n        // Healthcare-specific browser support\n        'Chrome >= 88', // For modern healthcare workstations\n        'Firefox >= 85', // For government healthcare systems\n        'Safari >= 14', // For Mac-based healthcare environments\n        'Edge >= 88', // For Windows healthcare systems\n      ],\n      // Grid support for complex healthcare layouts\n      grid: 'autoplace',\n      // Flexbox support for responsive healthcare UI\n      flexbox: 'no-2009',\n    },\n    \n    // CSS optimization for production builds\n    ...(process.env.NODE_ENV === 'production' && {\n      cssnano: {\n        preset: [\n          'default',\n          {\n            // Preserve important healthcare accessibility features\n            discardComments: {\n              removeAll: false, // Keep important healthcare comments\n            },\n            // Preserve calc() expressions for responsive healthcare layouts\n            calc: false,\n            // Preserve CSS custom properties for healthcare theming\n            customProperties: false,\n            // Optimize for healthcare performance requirements\n            normalizeWhitespace: true,\n            colormin: true,\n            convertValues: true,\n            discardDuplicates: true,\n            discardEmpty: true,\n            mergeIdents: false, // Preserve healthcare-specific identifiers\n            mergeLonghand: true,\n            mergeRules: true,\n            minifyFontValues: true,\n            minifyGradients: true,\n            minifyParams: true,\n            minifySelectors: true,\n            normalizeCharset: true,\n            normalizeDisplayValues: true,\n            normalizePositions: true,\n            normalizeRepeatStyle: true,\n            normalizeString: true,\n            normalizeTimingFunctions: true,\n            normalizeUnicode: true,\n            normalizeUrl: true,\n            orderedValues: true,\n            reduceIdents: false, // Preserve healthcare animation names\n            reduceInitial: true,\n            reduceTransforms: true,\n            svgo: {\n              // Preserve accessibility attributes in SVG icons\n              plugins: [\n                {\n                  name: 'preset-default',\n                  params: {\n                    overrides: {\n                      // Preserve IDs for healthcare icon accessibility\n                      cleanupIds: false,\n                      // Preserve aria attributes for healthcare compliance\n                      removeUnknownAttributes: {\n                        keepAriaAttrs: true,\n                        keepDataAttrs: true,\n                        keepRoleAttr: true,\n                      },\n                    },\n                  },\n                },\n              ],\n            },\n            uniqueSelectors: true,\n          },\n        ],\n      },\n    }),\n  },\n  \n  // Parser options for advanced CSS features\n  parser: undefined, // Use default parser\n  \n  // Map options for development debugging\n  map: process.env.NODE_ENV === 'development' ? {\n    inline: false,\n    annotation: true,\n    sourcesContent: true,\n  } : false,\n};\n\nexport default config;\n\n/**\n * Healthcare CSS Configuration Features:\n * \n * 1. Cross-Browser Compatibility:\n *    - Support for modern healthcare workstations\n *    - Government and enterprise browser requirements\n *    - Legacy system compatibility considerations\n *    - Mobile healthcare device support\n * \n * 2. Performance Optimization:\n *    - Minimized CSS for faster healthcare app loading\n *    - Optimized for emergency system response times\n *    - Reduced bundle size for mobile healthcare apps\n *    - Efficient caching for healthcare networks\n * \n * 3. Accessibility Preservation:\n *    - WCAG 2.1 AA compliance maintenance\n *    - Screen reader compatibility preservation\n *    - High contrast mode support\n *    - Healthcare-specific accessibility features\n * \n * 4. Security Considerations:\n *    - Safe CSS optimization practices\n *    - Preserved security-related CSS properties\n *    - Healthcare data display protection\n *    - No exposure of sensitive styling information\n * \n * 5. Development Experience:\n *    - Source map support for debugging\n *    - Clear error reporting\n *    - Healthcare design system integration\n *    - Consistent styling across environments\n */\n"
